name: Frist Workflow
on: workflow_dispatch
jobs:
  first-job:
   runs-on: ubuntu-latest
   steps:
     - name: Print checking
       run: echo "Checking"
     - name: Print code complete
       run: echo "Code check complete"
